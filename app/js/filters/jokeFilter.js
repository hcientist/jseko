/*global angular */
'use strict';

angular.module('jsekoApp')
.filter('JokeFilter', function () {
  // return function(params){alert('in JokeFiter: '+ params);};
// .filter('JokeFilter', function () {
  // $scope.kind = 'helen'
  // $scope.pcRange = function(item) {
  //   return (parseInt(item['min-acceptable-price']) >= $scope.lower_price_bound && parseInt(item['max-acceptable-price']) <= $scope.upper_price_bound);
  // };
});